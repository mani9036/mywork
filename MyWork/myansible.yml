---
- name: installing nginx and starting the service
  hosts : all
  become : true

  tasks:
      - name : install nginx
        yum :
            name : nginx
            state : latest
      - name : start nginx
        service :
            name: service
            state : started
            enabled: true